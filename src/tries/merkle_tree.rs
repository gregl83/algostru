struct Node {
    // todo
}

impl Node {
    pub fn new() -> Self {
        Node {

        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_tree() {
        // todo
        let mut tree = Node::new();
    }
}